# .env.test
# Configuration for the testing environment

DATABASE_URL="postgresql://postgres:12345@localhost:5432/fitflix_test_db" # Note the different port
PORT=3001 # Can be any port, tests usually don't need to be externally accessible
JWT_SECRET="superstrongrandomstring112233_test" # Use a test-specific secret
JWT_EXPIRES_IN="1h" # Shorter expiry for tests is fine
NODE_ENV="test"

# AWS S3 (use dummy values or test credentials for mocking)
AWS_ACCESS_KEY_ID="TEST_AWS_ACCESS_KEY_ID"
AWS_SECRET_ACCESS_KEY="TEST_AWS_SECRET_ACCESS_KEY"
AWS_REGION="us-east-1"
BUCKET_NAME="test-bucket"

# Twilio (use dummy values for mocking)
TWILIO_ACCOUNT_SID="TEST_TWILIO_ACCOUNT_SID"
TWILIO_AUTH_TOKEN="TEST_TWILIO_AUTH_TOKEN"
TWILIO_PHONE_NUMBER="+15005550006" # Twilio's test number

FRONTEND_URL="http://localhost:3000" # Not directly relevant for backend tests, but good to have